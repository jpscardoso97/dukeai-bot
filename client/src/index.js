import React from 'react';
import ReactDOM from 'react-dom';

import ChatWidget from './chatWidget';

const rootElement = document.getElementById('ai-blue-devil');
ReactDOM.render(<ChatWidget/>, rootElement);
